const { sendMail } = require('../config/sendMail');
const key = require('../config/key');

exports.signup = async (req, res, next) => {
	try {

        const {email,fname,lname,pwd,phNo}=req.body;
        
    }catch(err)
    {
    }
}